void setup(){
  // Declare and initialise some variables to represent the fruit.
  int apples = 12;
  int bananas = 7;
  int oranges = 6;
  // Find the total number of fruit.
  int totalNoFruit = apples + bananas + oranges;
  // Display the result using println().
  println(totalNoFruit);
}
